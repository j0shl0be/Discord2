# Tools

Helper scripts for migrations, seeding, and admin utilities.

- **Seed primary guild**: Create the initial guild and set its ID in `server/config.invite-single-guild.json`
- **Generate invite codes**: Use the Spacebar API or add a small script here to create invite codes
- **Admin CLI**: Optional scripts to manage users, roles, or settings from the command line
